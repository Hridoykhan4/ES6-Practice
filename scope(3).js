/* function add(a, b) {
  const total = a + b;
  if (b > 5) {
    const sum = 25 + a + b;
  } else {
    const sum = 5 + a + b;
    var current = sum
  }

console.log(current)
  return total;
}
// console.log(total)
add(5, 3);
 */

/* function add(a, b){
  const total = a + b;
  console.log(a, b);
  if(b > a){
    const sum = 25 + a + b
    return sum
  }else{
    const sum = a + b + 3;
    var current = sum;
  }
  return {current, total}
}

console.log(add(4,3)) */
